import React from 'react';

const Navbar = () => (
  <nav className="w-full flex justify-between items-center px-navbar h-navbar bg-primary-dark text-white">
    <a href="#home">Magalong</a>
    <div>Collapse</div>
  </nav>
);

export default Navbar;
